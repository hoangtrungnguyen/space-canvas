/// Enum to identify the type of node for serialization/deserialization factories.
enum NodeType {
  document,
  page,
  frame,
  rectangle,
  text,
  vector,
  group,
  connector,
  drawing,
  ellipse,
}
